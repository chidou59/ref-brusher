# Optional: identifies your requests to academic APIs that support a polite pool.
REF_BRUSHER_CONTACT_EMAIL=developer@example.com

# Optional: raises Semantic Scholar API limits when you have a key.
SEMANTIC_SCHOLAR_API_KEY=
